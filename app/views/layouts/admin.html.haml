!!!

%html
  %head
    %title LMS | #{@page_title || "Admin"}
    = stylesheet_link_tag 'application', media: 'all'
    = javascript_include_tag 'application', 'data-turbolinks-track' => true
    = csrf_meta_tags
  %body
    - if !flash[:notice].blank?
      .alert.alert-info= flash[:notice]

    .container
      #header
        %h1= " LMS : #{session[:admin] ? "Admin" : session[:username]}"
      #main

        .content
          = yield

    .footer
      .container
        %p.text-muted{ style: "text-align: right; font-size: .8em;" } LMS &copy; 2014 | JEA
